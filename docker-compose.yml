services:
  app:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CLOUDFLARE_API_TOKEN
    stdin_open: true
    tty: true
